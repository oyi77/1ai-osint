"""Plugin implementations package for 1ai-osint.

Drop a module here with a top-level ``plugin`` variable (a ``BasePlugin``
instance) and it will be auto-discovered by ``PluginRegistry.discover()``.
"""
