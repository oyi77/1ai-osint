"""MCP bridge package — blueprint Phase 1 (S3).

NOTE: named ``mcp_bridge`` (not ``mcp``) so it cannot shadow the official
``mcp`` MCP SDK package imported inside the server module. The FastMCP
instance lives in :mod:`src.mcp_bridge.server` as ``server``.
"""
