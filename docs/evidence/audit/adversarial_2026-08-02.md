# Adversarial verification receipt — 2026-08-02

Verdict: **PASS** (24/24 checks)

| check | passed | detail |
|---|---|---|
| ssrf_block:127.0.0.1:8080 | ✅ | '127.0.0.1:8080' is loopback IP literal — must be rejected |
| ssrf_block:http://127.0.0.1/ | ✅ | 'http://127.0.0.1/' is loopback inside URL — must be rejected |
| ssrf_block:169.254.169.254 | ✅ | '169.254.169.254' is AWS/GCP metadata link-local — must be rejected |
| ssrf_block:10.0.0.1 | ✅ | '10.0.0.1' is RFC1918 private — must be rejected |
| ssrf_block:192.168.1.1 | ✅ | '192.168.1.1' is RFC1918 private — must be rejected |
| ssrf_block:172.16.0.1 | ✅ | '172.16.0.1' is RFC1918 private — must be rejected |
| ssrf_block:0.0.0.0 | ✅ | '0.0.0.0' is unspecified — must be rejected |
| ssrf_block:2130706433 | ✅ | '2130706433' is decimal loopback alias (inet_aton) — must be rejected |
| ssrf_block:0x7f000001 | ✅ | '0x7f000001' is hex loopback alias — must be rejected |
| ssrf_block:localhost | ✅ | 'localhost' is blocked hostname — must be rejected |
| ssrf_block:metadata.google.internal | ✅ | 'metadata.google.internal' is cloud metadata hostname — must be rejected |
| ssrf_block:[::1]:8080 | ✅ | '[::1]:8080' is IPv6 loopback — must be rejected |
| ssrf_allow:https://8.8.8.8/x | ✅ | 'https://8.8.8.8/x' (public IP) — must be allowed |
| ssrf_allow:example.com | ✅ | 'example.com' (public hostname (DNS-resolvable)) — must be allowed |
| ssrf_allow:john_doe | ✅ | 'john_doe' (plain username — not host-like) — must be allowed |
| ssrf_allow:0xdeadbeef@example.org | ✅ | '0xdeadbeef@example.org' (email → public domain) — must be allowed |
| ssrf_allow:+6281234567890 | ✅ | '+6281234567890' (phone number) — must be allowed |
| ssrf_allow:bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh | ✅ | 'bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh' (crypto address) — must be allowed |
| ident_accept:../etc | ✅ | hostile-looking identifier '../etc' accepted as scan subject |
| ident_accept:<script>alert(1)</script | ✅ | hostile-looking identifier '<script>alert(1)</script>' accepted as scan subject |
| ident_accept:foo' OR '1'='1 | ✅ | hostile-looking identifier "foo' OR '1'='1" accepted as scan subject |
| ident_accept:evil"@example.com | ✅ | hostile-looking identifier 'evil"@example.com' accepted as scan subject |
| ident_accept:..%2f..%2fetc/passwd | ✅ | hostile-looking identifier '..%2f..%2fetc/passwd' accepted as scan subject |
| auth_fail_closed | ✅ | require_tier(ADMIN) with no resolved tier → 403, expected 403 (fail closed) |

Generated by `scripts/adversarial_check.py` — internal gate only; not a substitute for a third-party security audit.
