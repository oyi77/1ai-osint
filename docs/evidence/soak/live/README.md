# Live Soak Evidence — 2026-08-02

Soak receipts under `docs/evidence/soak/live/` are generated by `scripts/soak.py`
against the real `RateLimiter` and `Cache` implementations (temp dirs, no mocks),
with the soak verdict serialized as JSON on stdout.

## Completed run (PASS)

- Receipt: `soak_2026-08-02T014436Z.json`
- Command: `uv run python scripts/soak.py --duration 60 --json`
- Verdict: **PASS** (exit 0) — 170,674 calls, 0 errors, p95 0.20 ms (≤ 50 ms),
  uptime 100%, 60.4 s elapsed.

## Interrupted sustained run (1 h, networked)

An attempted 1-hour networked soak (`--duration 3600 --network --long-run`) was
launched at ~01:08Z on 2026-08-02 to gather sustained-run evidence. It completed
two full 10-minute epochs cleanly before the host killed it externally:

- `[epoch 1] rate_limiter: calls=10947 errors=0 @ 01:18:49Z`
- `[epoch 2] rate_limiter: calls=21747 errors=0 @ 01:28:49Z`
- Then it was mid `[cache] set/get/has/delete/prune...` phase when the process
  died — no traceback, no `exit=` line. `dmesg` shows the host OOM-killed a
  `chrome-headless` process in an unrelated service cgroup (`1ai-career.service`)
  while system swap was fully exhausted (swap 21697/21697 MB, load 23.14).
  The soak code was healthy: 2 epochs, 21,747 rate-limiter calls, **0 errors**,
  all under external host memory pressure.

The 0-byte receipt from that interrupted attempt was deleted; this note is the
record of it. Sustained long-run soak coverage is handled by the nightly CI cron
(`.github/workflows/soak.yml`, `--duration 3600 --network --long-run`), which
produces the longer-term receipt on a host with a dedicated runner allocation.
